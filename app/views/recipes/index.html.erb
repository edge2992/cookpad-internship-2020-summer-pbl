<h1>Zoom飲み会でよく作られているレシピ一覧ページ</h1>
<table>
  <tr>
    <th>料理名</th>
    <th>レシピへのリンク</th>
    <th>料理された回数</th>
  </tr>

  <% @recipes.each do |recipe| %>
    <tr>
      <td><%= recipe.title %></td>
      <td><%= link_to 'Show', recipe.url %></td>
      <td><%= recipe.frequency %></td> 
    </tr>
  <% end %>
</table>

<%= link_to 'New Zoom', new_zoom_path %>
<%= link_to 'Zoom一覧', zooms_path %>
<%= link_to 'レシピ一覧', recipes_path %>